[package]
name = "delta_kernel_benchmarks"
publish = false
edition.workspace = true
homepage.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true
rust-version.workspace = true

[features]
default = ["arrow"]
arrow = ["arrow-59"]
arrow-59 = ["delta_kernel/arrow-59", "delta_kernel_workloads/arrow-59"]
arrow-58 = ["delta_kernel/arrow-58", "delta_kernel_workloads/arrow-58"]

[build-dependencies]
flate2 = "1.1"
sha2 = "0.10"
tar = "0.4"
ureq = { version = "3.1", default-features = false, features = ["native-tls", "gzip"] }

[dependencies]
delta_kernel = { path = "../kernel", features = ["default-engine-base", "internal-api"] }
delta_kernel_default_engine = { path = "../default-engine", features = ["rustls"] }
delta_kernel_workloads = { path = "../workloads" }
delta-kernel-unity-catalog = { path = "../delta-kernel-unity-catalog" }
object_store = { version = "0.12.3", features = ["aws"] }
test_utils = { path = "../test-utils" }
rayon = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread"] }
unity-catalog-delta-client-api = { path = "../unity-catalog-delta-client-api" }
unity-catalog-delta-rest-client = { path = "../unity-catalog-delta-rest-client" }
url = { version = "2", features = ["serde"] }

[dev-dependencies]
criterion = "0.5"
tempfile = "3"

[[bench]]
name = "workload_bench"
harness = false
