[package]
name = "acceptance"
publish = false
edition.workspace = true
homepage.workspace = true
keywords.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
version.workspace = true
rust-version.workspace = true

# for cargo-release
[package.metadata.release]
release = false

[features]
default = ["arrow"]
arrow = ["arrow-59"]
arrow-59 = ["delta_kernel/arrow-59", "delta_kernel_workloads/arrow-59", "test_utils/arrow-59"]
arrow-58 = ["delta_kernel/arrow-58", "delta_kernel_workloads/arrow-58", "test_utils/arrow-58"]

[dependencies]
delta_kernel = { path = "../kernel", features = [
  "default-engine-base",
  "internal-api",
  "prettyprint",
] }
delta_kernel_default_engine = { path = "../default-engine", features = ["rustls"] }
delta_kernel_workloads = { path = "../workloads" }
futures = "0.3"
itertools = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = { version = "0.1", default-features = false }
url = "2"

[build-dependencies]
# native-tls validates against the OS trust store (like curl), so build-script downloads work
# behind a sanctioned TLS-intercepting corporate proxy whose CA isn't in ureq's bundled
# webpki-roots. See build.rs `build_agent`.
ureq = { version = "3.1", default-features = false, features = ["native-tls", "gzip"] }
flate2 = "1.1"
sha2 = "0.10"
tar = "0.4"

[dev-dependencies]
datatest-stable = "0.3"
test-log = { version = "0.2", default-features = false, features = ["trace"] }
test_utils = { path = "../test-utils", default-features = false }
tempfile = "3"
test-case = { version = "3.3.1" }
tokio = { version = "1.47" }
tracing-subscriber = { version = "0.3", default-features = false, features = [
  "env-filter",
  "fmt",
] }

[[test]]
name = "dat_reader"
harness = false

[[test]]
name = "acceptance_workloads_reader"
harness = false
