[package]
name = "test_utils"
edition.workspace = true
homepage.workspace = true
keywords.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
version.workspace = true

# for cargo-release
[package.metadata.release]
release = false

[features]
default = ["arrow"]
arrow = ["arrow-59"]
arrow-59 = ["delta_kernel/arrow-59", "delta_kernel_default_engine/arrow-59"]
arrow-58 = ["delta_kernel/arrow-58", "delta_kernel_default_engine/arrow-58"]

[dependencies]
# NOTE: We MUST build with SOME arrow version (or compilation would fail). So we pull in `arrow-58`,
# which ensures `cargo build` succeeds. But we ALSO default to `arrow`, which activates `arrow-59`
# and overrides `arrow-58`.
delta_kernel = { path = "../kernel", features = [ "arrow-58", "internal-api", "prettyprint", "test-utils" ] }
# Re-exported so kernel's integration tests (which cannot dev-dep default-engine directly due to
# the cycle through this crate) can reach it via `test_utils::delta_kernel_default_engine::*`.
delta_kernel_default_engine = { path = "../default-engine", default-features = false, features = ["arrow-58", "rustls"] }
itertools = "0.14.0"
serde_json = "1.0.142"
tar = "0.4"
tempfile = "3"
url = "2.5.4"
uuid = { version = "1", features = ["v4"] }
zstd = "0.13"
tokio = "1.49.0"
# only for structured logging
tracing = { version = "0.1", features = ["log"] }
tracing-subscriber = { version = "0.3", default-features = false, features = [
  "env-filter",
  "fmt",
] }

[dev-dependencies]
rstest = "0.23"
rstest_reuse = "0.7"

# rstest/rstest_reuse are also needed as regular deps so that rstest_reuse templates
# defined in table_builder.rs (non-test code) can be exported to other crates' tests.
[dependencies.rstest]
version = "0.23"

[dependencies.rstest_reuse]
version = "0.7"
