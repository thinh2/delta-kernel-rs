[package]
name = "unity-catalog-delta-rest-client"
description = "REST/HTTP client for the Unity Catalog Delta Tables API."
edition.workspace = true
homepage.workspace = true
keywords.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
rust-version.workspace = true
version = "0.1.0"

# for cargo-release
[package.metadata.release]
release = false

[dependencies]
unity-catalog-delta-client-api = { path = "../unity-catalog-delta-client-api", version = "0.1.0" }
# Hardcodes rustls (always used alongside default-engine-rustls in practice).
reqwest = { version = "0.13", default-features = false, features = ["charset", "http2", "json", "rustls", "system-proxy"] }
percent-encoding = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
thiserror = "2.0"
tracing = "0.1"
url = "2.5"

[features]
default = []
test-utils = ["unity-catalog-delta-client-api/test-utils"]
# Gates live integration tests that hit a real Unity Catalog server. Off by default so normal
# `cargo test` never compiles or runs them; the dedicated CI job enables it. See
# tests/integration_live_server.rs.
integration-test = []
